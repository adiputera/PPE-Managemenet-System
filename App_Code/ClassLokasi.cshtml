@using System.Dynamic;
@functions{
   public class lokasi{
        //select
        public static dynamic SelectLokasi {
            get{
            var sql = "SELECT idLokasi, namaLokasi FROM lokasi";
            return AccessData.ppe1.Query(sql);
            }
        }

        public static dynamic SelectNalok(string id){
            var sql = "select namaLokasi from lokasi where idLokasi=@0";
            return AccessData.ppe1.QuerySingle(sql, id);
        }
    }

}